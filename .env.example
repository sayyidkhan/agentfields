###############################################################################
# AgentField example env file (.env.example)
#
# Copy to .env.local and fill in real values:
#   cp .env.example .env.local
###############################################################################

###############################################################################
# AgentField control plane (af server)
###############################################################################

# Optional: only needed if you want to configure the control plane via env.
# AGENTFIELD_PORT=8080
# AGENTFIELD_MODE=local
# AGENTFIELD_STORAGE_MODE=local

###############################################################################
# Your agent
###############################################################################

# Control plane URL your agent connects to
AGENTFIELD_SERVER=http://localhost:8080

# Common agent metadata (optional but recommended)
NODE_ID=my-agent
DEV_MODE=true

# Port your agent serves on (var name depends on your code/framework)
PORT=8001

###############################################################################
# OpenAI
###############################################################################

# Required for OpenAI-backed reasoners (put your real key in .env.local)
OPENAI_API_KEY=sk-REPLACE_ME

# Optional: if your code reads these (many templates hardcode the model in code)
# OPENAI_MODEL=openai/gpt-4o
# OPENAI_TEMPERATURE=0.7

