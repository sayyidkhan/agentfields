# MagiStock Frontend — Environment Variables
#
# Copy to .env.local and fill in your values:
#   cp .env.example .env.local
#
# NOTE: Anything used in the frontend is available to browser code.
# For local development, this project supports either:
# - VITE_OPENAI_API_KEY (recommended), or
# - OPENAI_API_KEY (backwards compatible)
#
# You must restart `npm run dev` after editing env files.

# Required for the Magi chat flow
VITE_OPENAI_API_KEY=sk-REPLACE_ME

# Optional
VITE_OPENAI_MODEL=gpt-4o-mini

